using System;
using NSWallet.Shared;
using NSWallet.Shared.Helpers.Logs;
using NSWallet.Shared.Helpers.Logs.AppLog;
using NUnit.Framework;

namespace NSWallet.UnitTests
{
	/*
	[TestFixture]
	public class LogsFixture
	{
		const string message = "message";
		const string methodName = "methodName";
		const string className = "className";
		const double maxFileSizeMB = 2;

		[Test]
		public void CheckLoggerNullableFilepath()
		{
			var logger = new Logger(null, 1, null);
			var isLogged = logger.Log(message, methodName, className);
			Assert.AreEqual(false, isLogged);
		}

		[Test]
		public void CheckAppLogsNullableFilepath()
		{
			AppLogs.Init(null, null, true);
			var isLogged = AppLogs.Log(message, methodName, className);
			Assert.AreEqual(false, isLogged);
		}
	}
	*/
}